<!DOCTYPE html>
<html lang="es">
<head>
    <title>Aceros</title>

    <!-- Meta -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Aneimera">
    <meta name="author" content="Albert Castellano Moreno">
</head> 

<body>
{% for type, messages in app.session.flashbag.all() %}
		  {% for message in messages %}
			  <div class="alert alert-{{ type }}">
				  <strong>{{ message|trans({}, 'FOSUserBundle') }}</strong>
			  </div>
		  {% endfor %}
	  	{% endfor %}
<form action="#" method="post" {{ form_enctype(form) }}>
	<div class="row-fluid">
    	{{ form_widget(form) }}
    	<h5>* Campos Obligatorios</5>
	</div>
    <div class="actions">
		<input type="submit" class="btn btn-primary span12" value="Registrate" />
    </div>
</form>
</body>
</html> 